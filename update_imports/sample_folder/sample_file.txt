import React, {ChangeEvent, Dispatch, FocusEvent, SetStateAction, useRef, useState} from 'react';
import {useLocation, useNavigate} from 'react-router-dom';

import {
  Box,
  Divider,
  Paper,
  makeStyles,
  Table,
  TableHead,
  TableRow,
  TableCell,
  TableBody,
  TablePagination,
  TableContainer,
  TableSortLabel,
  TextField,
} from '@material-ui/core';

import html2canvas from 'html2canvas';
import AuthService from '../../network/AuthService';
import BookingDetailsModal from './BookingDetails/BookingDetailsModal';
import BookingDetailsView from './BookingDetails/BookingDetailsView';
import {BookingFilters} from './BookingsHeader';
import BookingListItem from './BookingListItem';
import BookingListOptions from './BookingListOptions';
import BookingResource from '../../network/resources/BookingResource';
// import BookingSearchBar from './BookingSearchBar';
import BookingSkeletonItem from './BookingSkeletonItem';
import BookingsDownloadDialog from './BookingsDownloadDialog';
// import Counter from '../../models/Counter';
import DataExporter from '../../exports/DataExporter';
import EmptyBookingTable from './EmptyBookingTable';
import FirebaseService from '../../network/FirebaseService';
import ImageCanvasDialog from '../common/ImageCanvasDialog';
import LiveData from '../../components/common/LiveData';
import Logger from '../../common/Logger';
import NavUtils, {URLParamKey} from '../../utils/NavU

The linecache package can be imported in Python and then be used to extract and access specific lines in Python. 
The package can be used to read multiple lines simultaneously. 
It makes use of cache storage to perform optimization internally.
This package opens the file on its own and gets to the particular line. 
This package has getline() method which is used for the same. 